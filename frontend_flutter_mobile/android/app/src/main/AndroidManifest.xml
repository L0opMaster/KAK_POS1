<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Added Day 8 for mobile_scanner (camera-based barcode scanning) —
         same permission frontend-flutter-pos and
         frontend_flutter_barcode_scanner both already declare. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Flagged as a gap in DAY_02_MOBILE_PROJECT_CONFIGURATION.md
         ("a release build of this POS app... will have no INTERNET
         permission and all network calls will fail at the OS level"),
         deliberately deferred until real network calls were wired up.
         That's now true of every screen in the app — added here so a
         release build can actually reach the backend (the debug-only
         manifest overlay already grants this for `flutter run`, but that
         overlay is stripped from release builds). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Added Day 16 for BluetoothPrinterTransport (classic Bluetooth
         connection to a paired thermal printer) — confirmed absent from
         frontend-flutter-pos too, genuinely new for both projects.
         BLUETOOTH_CONNECT/SCAN are the Android 12+ (API 31+) runtime
         permissions; permission_handler's Permission.bluetoothConnect
         (see printer_permission.dart) requests this at connect time. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <application
        android:label="KAKNNEA MOBILE POS"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
