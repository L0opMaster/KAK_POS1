name: frontend_flutter_mobile
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # State management (matches frontend-flutter-pos architecture)
  flutter_riverpod: ^2.6.1

  # Local persistence (theme/main-color/language selection)
  shared_preferences: ^2.5.3

  # HTTP client for the shared Spring Boot backend (matches
  # frontend-flutter-pos's api_service.dart — same Dio-based wrapper,
  # same auth-token interceptor). Added Day 4.
  dio: ^5.3.3

  # Camera-based barcode scanning — same package/version
  # frontend-flutter-pos and frontend_flutter_barcode_scanner both already
  # depend on. Added Day 8.
  mobile_scanner: ^7.4.0

  # Pinned explicitly (matches frontend-flutter-pos) to avoid version drift
  # with the flutter_localizations-generated code.
  intl: ^0.20.2

  # Idempotency-key generation for checkout (`PaymentScreen._clientRef`) —
  # same version frontend-flutter-pos depends on. Added Day 11.
  uuid: ^4.2.1

  # PDF receipt generation + OS print dialog/share sheet — same versions
  # frontend-flutter-pos depends on. Added Day 13. This pipeline is
  # confirmed platform-portable (zero platform-conditional code needed).
  pdf: ^3.10.4
  printing: ^5.12.0

  # Raw pixel manipulation for the Khmer receipt bitmap path
  # (ReceiptBitmapRenderer decodes to img.Image; EscPosReceiptBuilder
  # Floyd-Steinberg-dithers it for 1-bit thermal output). Same version
  # frontend-flutter-pos depends on. Added Day 14.
  image: ^4.3.0

  # Raw ESC/POS byte building (Generator/CapabilityProfile) for the
  # thermal (bluetooth/usb/network) print path. Same version
  # frontend-flutter-pos depends on. Added Day 15.
  esc_pos_utils_plus: ^2.0.4

  # Bluetooth (classic) thermal printer transport. Same version
  # frontend-flutter-pos depends on. Added Day 16.
  print_bluetooth_thermal: ^1.2.1

  # USB thermal printer transport (Android). Same version
  # frontend-flutter-pos depends on. Added Day 16.
  flutter_pos_printer_platform_image_3: ^1.2.4

  # Runtime permission requests (Bluetooth) — genuinely new in this
  # project; frontend-flutter-pos declares this dependency too but never
  # actually imports it anywhere (confirmed by grep). Added Day 16.
  permission_handler: ^11.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # Enables `flutter gen-l10n` (config in l10n.yaml) to regenerate
  # lib/l10n/generated/app_localizations.dart from the ARB files below.
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  assets:
    - assets/fonts/

  # Ported from frontend-flutter-pos/pubspec.yaml — same two font families,
  # same fallback-only Khmer usage pattern (see Day 3 doc for why NotoSansKhmer
  # is never used as a primary/body font).
  fonts:
    - family: NotoSans
      fonts:
        - asset: assets/fonts/NotoSans-Regular.ttf
        - asset: assets/fonts/NotoSans-Bold.ttf
          weight: 700
    - family: NotoSansKhmer
      fonts:
        - asset: assets/fonts/NotoSansKhmer-Regular.ttf
        - asset: assets/fonts/NotoSansKhmer-Bold.ttf
          weight: 700

  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
