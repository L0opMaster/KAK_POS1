// import 'package:flutter_test/flutter_test.dart';

// void main() {
//   group('Login Email Validation', () {
//     test('owner@kaknnea.local should be valid', () {
//       // Regex updated to match LoginScreen implementation
//       final regex = RegExp(r'^[\w-\.]+@([\w-]+\.)+[\w-]{2,}$');

//       expect(
//         regex.hasMatch('owner@kaknnea.local'),
//         isTrue,
//         reason: 'Should accept .local domains',
//       );
//       expect(regex.hasMatch('user@example.com'), isTrue);
//     });
//   });
// }
